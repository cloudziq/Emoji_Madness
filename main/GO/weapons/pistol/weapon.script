function init(self)
	timer.delay(0.04, false, function () go.delete() end)
end




function update(self, dt)
	if weapon_collide == true then
		msg.post("#pistol", "enable")
	else
		msg.post("#pistol", "disable")
	end
end
